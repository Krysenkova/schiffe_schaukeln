class Schiff {
  float xPosition;
  float yPosition;
  PImage ship;
  
  Schiff(float xPos, float yPos){
  xPosition = xPos;
  yPosition = yPos;
}

void display(){
  ship=loadImage("ship.png");
  image(ship, xPosition, yPosition,300,200);
}
}
